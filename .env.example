# Copy to .env (gitignored) for local `npx netlify dev`.
# Get a key at https://aistudio.google.com/apikey
# For production, set the same variable in Netlify: Site settings -> Environment variables.
GEMINI_API_KEY=
